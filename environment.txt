# Conda package specification for this project.
#
# Create the environment with:
#   conda create -n mne-eeg -c conda-forge --file environment.txt
#   conda activate mne-eeg
#
# The original environment versions were not stored in the repository.
# These constraints are based on the packages used by the notebooks.

python=3.11
pip

# Core scientific stack
numpy>=1.24,<3.0
scipy>=1.10,<2.0
pandas>=2.0,<3.0
matplotlib>=3.7,<4.0
seaborn>=0.13,<1.0
networkx>=3.1,<4.0
tqdm>=4.65,<5.0

# EEG / neuroimaging
mne>=1.6,<2.0
nibabel>=5.1,<6.0

# Machine learning
scikit-learn>=1.3,<2.0

# MNE 3D visualization
pyvista>=0.43,<1.0
pyvistaqt>=0.11,<1.0
qtpy>=2.4,<3.0

# Notebook environment
jupyterlab>=4.0,<5.0
ipykernel>=6.25,<8.0
